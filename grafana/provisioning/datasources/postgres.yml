apiVersion: 1

datasources:
  - name: Sport PostgreSQL
    type: postgres
    url: postgres:5432
    database: sport
    user: ${POSTGRES_USER}
    secureJsonData:
      password: ${POSTGRES_PASSWORD}
    jsonData:
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false
    isDefault: true
